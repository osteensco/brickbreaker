Build out game loop


Set up dynamic sizes


Develop level generation algorithm for random levels and set designs for set levels
    -Infinite scaling:
        -levels are a grid system, 0 or 1 values for a block placed or not placed
        -a value > 0 acts as block's hp, block looses 1 hp per ball hit
        -for each iteration of a level group these values can be +1

Build other menu components
    -high scores
    -settings

Set up cached/saved settings capability
    -explore data persistancy options



